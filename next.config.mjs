/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: [
      'www.google.com',
      'your-api-domain.com', // include your backend image domain if needed
    ],
  },
};

export default nextConfig;
